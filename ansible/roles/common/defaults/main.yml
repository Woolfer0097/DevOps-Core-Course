---
common_packages:
  - python3-pip
  - curl
  - git
  - vim
  - htop
  - ca-certificates
  - gnupg
  - lsb-release

common_timezone: "UTC"

common_users:
  - name: devops
    groups: [sudo]
    shell: /bin/bash
