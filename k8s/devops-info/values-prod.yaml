replicaCount: 5

image:
  tag: "1.0.0"

service:
  type: NodePort
  nodePort: 30081

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 200m
    memory: 256Mi

probes:
  liveness:
    initialDelaySeconds: 30
    periodSeconds: 5
  readiness:
    initialDelaySeconds: 10
    periodSeconds: 3
